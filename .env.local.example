# Copy this file to `.env.local` and fill in the values before running the app.
# Values can be found in the Amplify console (Backend > Data > API details)
# or inside the generated `amplify_outputs.json` if you have pulled the backend locally.

AMPLIFY_DATA_GRAPHQL_ENDPOINT=https://your-appsync-id.appsync-api.your-region.amazonaws.com/graphql
AMPLIFY_DATA_REGION=ap-northeast-1
AMPLIFY_DATA_API_KEY=yourAmplifyDataApiKey

# Optional: choose a different auth mode (apiKey, iam, oidc, userPool, lambda).
# Leave blank to default to apiKey.
# AMPLIFY_DATA_AUTH_MODE=apiKey
